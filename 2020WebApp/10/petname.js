const collection = ["あ", "い", "う", "え", "お", "か", "き", "く", "け", "こ"];

for (let i = 0; i < collection.length; i++) {
  for (let j = 0; j < collection.length; j++) {
    document.write("<p>" + collection[i] + collection[j] + "</p?");
  }
}
